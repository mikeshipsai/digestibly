[project]
name = "digestibly"
version = "0.1.0"
description = "Digestibly — personal Telegram digest bot"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "telethon>=1.36",
    "aiogram>=3.10",
    "apscheduler>=3.10",
    "python-dotenv>=1.0",
    "aiohttp>=3.9",
]
